"Use strict"; // необходимая запись

const options = { // объект
    name: "test", // "name:"" - свойство объекта
    height: 1024, // в конце запятая
    color: { // вложенная структура, объект внутри объекта
        border: "black",
    }
}